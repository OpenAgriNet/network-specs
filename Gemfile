source "https://rubygems.org"

gem "github-pages", group: :jekyll_plugins
gem "webrick"

# Keep local builds usable with the macOS system Ruby without constraining newer Ruby installations.
gem "ffi", "< 1.17" if RUBY_VERSION < "3.0"
